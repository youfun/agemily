name: agemily
description: "Cross-platform AI chat agent"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^2.6.0
  go_router: ^14.6.0
  drift: ^2.22.0
  sqlite3_flutter_libs: ^0.5.28
  sqlite3: ^2.9.0
  path_provider: ^2.1.0
  path: ^1.9.0
  flutter_secure_storage: ^9.2.0
  flutter_markdown: ^0.7.4
  flutter_chat_ui: ^1.6.15
  flutter_chat_types: ^3.6.2
  image_picker: ^1.1.0
  dio: ^5.7.0
  uuid: ^4.5.1
  intl: ^0.20.2
  cupertino_icons: ^1.0.8
  flutter_tts: ^4.2.0
  connectivity_plus: ^6.1.0
  flutter_dotenv: ^6.0.0
  url_launcher: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.22.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - .env
    - .env.example
    - assets/logo.png
